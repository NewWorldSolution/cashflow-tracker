
  # Minimal Transaction Entry Form

  This is a code bundle for Minimal Transaction Entry Form. The original project is available at https://www.figma.com/design/bQB25wNTRo4nfF4DrcDMsO/Minimal-Transaction-Entry-Form.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  